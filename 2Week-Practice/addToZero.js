
console.log ("--------------------------------")
console.log ('Up (true) or Down (false)?')
let array = [28, 43, -12, 30, 4, 0, 12, 5, 11];
let x = ('Down ' + false);
    for (let i = 0; i < array.length; i++) 
        {
        for (let k = 0; k < array.length; k++) {;
        if (array[i] + array[k] === 0) {;
        x = ('Up ' + true);
        }; 
    };
};
console.log (x); 




